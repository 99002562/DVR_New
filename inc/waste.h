/**
* @file biosensor.h
*
*/
#ifndef __WASTE_H__
#define __WASTE_H__

/**
*read the bio sensor values from file
* @return True(1)for successful updation else return False(0)
* @note Returns 0 if test case fails
*/
int prime(int number);
int check(char t[10]);



#endif /* #ifndef __PRIME_H__ */
